openapi: 3.0.0
info:
  title: aicc-analyzer
  description: Analyzer service for contract compliance evaluation, document processing, and rule scoring.
  version: 1.0.0
paths:
  /:
    get:
      summary: Root endpoint
      responses:
        '200':
          description: Successful response
  /health:
    get:
      summary: Health check endpoint
      responses:
        '200':
          description: Service is healthy
        '503':
          description: Service is unhealthy
